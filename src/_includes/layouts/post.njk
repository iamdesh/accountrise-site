<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>{{ title }} | Accountrise</title>
  <meta name="description" content="{{ excerpt | striptags | truncate(160, true, '…') }}">
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 text-gray-900 font-sans">
  <nav class="w-full bg-gradient-to-r from-indigo-500 to-indigo-300 text-white shadow-md py-6">
    <div class="max-w-7xl mx-auto flex justify-between items-center px-4">
      <h1 class="text-3xl font-extrabold">Accountrise</h1>
      <a href="/blog/" class="hover:underline">← Back to Blog</a>
    </div>
  </nav>

  <main class="max-w-3xl mx-auto py-12 px-6 bg-white rounded shadow">
    <article>
      <h1 class="text-3xl font-bold text-indigo-600 mb-3">{{ title }}</h1>
      <p class="text-gray-500 text-sm mb-6">{{ page.date | readableDate }} • {{ category }}</p>

      {% if thumbnail %}
        <img src="{{ thumbnail }}" alt="{{ title }}" class="rounded mb-6">
      {% endif %}

      <div class="prose prose-lg max-w-none">
        {{ content | safe }}
      </div>

      <div class="mt-10">
        <a href="/blog/" class="text-indigo-600 hover:underline">← Back to All Posts</a>
      </div>
    </article>
  </main>

  <footer class="bg-indigo-700 text-white text-center py-6 mt-8">
    <p>&copy; 2025 Accountrise. All rights reserved.</p>
  </footer>
</body>
</html>
