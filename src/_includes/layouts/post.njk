---
layout: layouts/base.njk
---

<main class="max-w-3xl mx-auto py-12 px-4">
  <a href="/" class="text-indigo-600 hover:underline">← Back to Home</a>
  <h1 class="text-4xl font-extrabold mt-4 mb-2">{{ title }}</h1>
  {% if description %}<p class="text-gray-600 mb-6">{{ description }}</p>{% endif %}
  <article class="prose prose-indigo max-w-none">
    {{ content | safe }}
  </article>
</main>
